{
  "assignee": "",
  "blocked_by": [],
  "blocks": [
    "sbdpi-qmd.1.5"
  ],
  "created": "2026-05-08T23:35:07Z",
  "description": "Add the `workerExecution` configuration block that controls which execution substrate is used for new workers.\n\n## What to implement\n\nIn `packages/pi-beadwork-extension/src/config.ts`:\n\n```ts\nworkerExecution: {\n  mode: \"current-branch\" | \"worktree\";  // default: \"worktree\" initially (flipped in Phase 5)\n  maxLifetime: number | null;           // ms, default: null (no limit)\n}\n```\n\nEnvironment override: `PI_BEADWORK_WORKER_EXECUTION_MODE=current-branch|worktree`\n\n## Design Rules\n\n- Default to `\"worktree\"` during development (Phase 5 flips to `\"current-branch\"`)\n- Env override takes precedence over config file\n- `maxLifetime` is for crash detection timeout; `null` means rely only on supervisor polling\n- Existing `worktrees` config remains but applies ONLY when mode is `worktree`\n- Do NOT let worktree-specific config (`copyFiles`, `setupCommands`, `cleanup`, `rerunSetupOnReuse`) leak into current-branch behavior\n- Current-branch review defaults to ON; `landing.review.enabled = false` should NOT disable current-branch review (that's a worktree landing setting)\n\n## Isolation Semantics\n\nWhen `mode = \"current-branch\"`:\n- `worktrees.copyFiles` → ignored\n- `worktrees.setupCommands` → ignored\n- `worktrees.cleanup` → ignored\n- `worktrees.rerunSetupOnReuse` → ignored\n- `landing.review.enabled` → does NOT control current-branch review (separate concern)\n\n## Tests\n\n- Config loads defaults correctly\n- Env override works for both modes\n- `maxLifetime` parses null and numeric values\n- Worktree config is not consulted when mode is `current-branch`\n\n## Definition of Done\n\n- Config block loads from file and env\n- Type-safe access to `config.workerExecution.mode`\n- Isolation rules enforced at config resolution time\n- Tests cover all cases\n- lint/test/typecheck pass clean",
  "id": "sbdpi-qmd.1.2",
  "labels": [],
  "parent": "sbdpi-qmd.1",
  "priority": 1,
  "status": "open",
  "title": "Add workerExecution config block with mode and maxLifetime",
  "type": "task",
  "updated_at": "2026-05-08T23:35:16Z"
}
