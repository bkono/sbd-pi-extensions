# Current-branch e2e smoke scripts

**Removed.** The tmux/fake-worker current-branch swarm scripts are not the published cutover
path.

In-process minions + `/bw run` coverage lives with the goal-mode fixtures (scripted parent, no
tmux). Operator docs: [../README.md](../README.md) and [migration.md](./migration.md).
